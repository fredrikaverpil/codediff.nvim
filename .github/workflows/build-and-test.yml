name: Build and Test

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build and Test (${{ matrix.os }}-${{ matrix.arch }})
    runs-on: ${{ matrix.runs-on }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x64
          - os: ubuntu-latest
            arch: x64
            runs-on: ubuntu-latest
          # macOS x64 (Intel)
          - os: macos-13
            arch: x64
            runs-on: macos-13
          # macOS ARM64 (Apple Silicon M1)
          - os: macos-latest
            arch: arm64
            runs-on: macos-latest
          # Windows x64
          - os: windows-latest
            arch: x64
            runs-on: windows-latest
    # Note: ARM64 support on Linux and Windows requires GitHub Team/Enterprise plan
    # The C code is portable and will build correctly on ARM64 when native runners are available
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Install build tools (Ubuntu)
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake

      - name: Install Neovim (Ubuntu)
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository ppa:neovim-ppa/unstable -y
          sudo apt-get update
          sudo apt-get install -y neovim
          nvim --version | head -n 1

      - name: Install Neovim (macOS)
        if: startsWith(matrix.os, 'macos')
        run: |
          brew install --HEAD neovim
          nvim --version | head -n 1

      - name: Install Neovim (Windows)
        if: startsWith(matrix.os, 'windows')
        shell: pwsh
        run: |
          choco install neovim --pre -y
          $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
          nvim --version | Select-Object -First 1

      - name: Build library (Ubuntu/macOS)
        if: startsWith(matrix.os, 'ubuntu') || startsWith(matrix.os, 'macos')
        run: |
          make clean || true
          make

      - name: Build library (Windows)
        if: startsWith(matrix.os, 'windows')
        run: |
          .\build.cmd

      - name: Run C unit tests (Ubuntu/macOS)
        if: startsWith(matrix.os, 'ubuntu') || startsWith(matrix.os, 'macos')
        run: |
          make test-c

      - name: Run C unit tests (Windows)
        if: startsWith(matrix.os, 'windows')
        shell: cmd
        run: |
          cmake -B build
          cmake --build build --config Release
          cd build
          ctest -C Release --output-on-failure

      - name: Run Neovim tests (Ubuntu/macOS)
        if: startsWith(matrix.os, 'ubuntu') || startsWith(matrix.os, 'macos')
        run: |
          chmod +x tests/run_tests.sh
          tests/run_tests.sh

      - name: Run Neovim tests (Windows)
        if: startsWith(matrix.os, 'windows')
        shell: bash
        run: |
          export PATH="/c/tools/neovim/nvim-win64/bin:$PATH"
          tests/run_tests.sh

      - name: Upload build artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: libvscode-diff-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            libvscode_diff.so
            libvscode_diff.dylib
            libvscode_diff.dll
          if-no-files-found: ignore
          retention-days: 7
